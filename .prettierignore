node_modules/
dist/
.next/
coverage/
pnpm-lock.yaml
apps/api/prisma/migrations/
docs/prediction-market-demo.html
# The design reference — the source of truth every screen is checked against.
# Reformatting it would churn the one file whose exact markup is the spec, and
# lint-staged never caught it locally because its globs do not include .html
# while `prettier --check .` checks everything.
docs/design-reference.html
docs/platform-architecture.md
docs/market-rulebook.md
docs/build-plan.md
docs/product-brief.md
apps/web/public/spcoin.svg
scripts/*.py

# Vendored from anthropics/skills, verbatim and under Apache 2.0. The whole
# value of a vendored copy is that `diff` against upstream is empty, so the next
# person can tell what is ours and what is theirs. Prettier reformatted 63 of
# these files on the commit that added them and quietly made that untrue.
#
# Our own skills are NOT excluded — those we do format.
.claude/skills/claude-api/
.claude/skills/frontend-design/
.claude/skills/skill-creator/

# Generated by @serwist/next at build time — not source.
apps/web/public/sw.js
apps/web/public/sw.js.map
